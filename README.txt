make sure node is installed on the server

cd to directory

2- npm install

3- node index

4- go to http://localhost:3000, the server should be running on local IP.

5- you can change the port in index.js to whatever you'd like. port 80 is default for http, port 443 is default for https.